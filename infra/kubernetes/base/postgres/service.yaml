apiVersion: v1
kind: Service
metadata:
  name: quantlix-assistant-postgres
  namespace: quantlix
  labels:
    app: quantlix-assistant-postgres
spec:
  selector:
    app: quantlix-assistant-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
