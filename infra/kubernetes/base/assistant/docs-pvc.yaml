apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quantlix-assistant-docs
  namespace: quantlix
  labels:
    app: quantlix-assistant-docs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 10Gi
